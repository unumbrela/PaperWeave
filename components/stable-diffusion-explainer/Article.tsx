export function Article() {
  return (
    <article className="prose-ai mx-auto mt-16 max-w-[72ch]">
      <h2>把扩散模型讲清楚</h2>
      <p>
        像 Stable Diffusion 这样的「文生图」模型，核心只有一个朴素的想法：
        <strong>既然往一张图里慢慢加噪声很容易，那么训练一个网络去把噪声一点点拿掉，
        不就能从纯噪声里「画」出图来了吗？</strong>
        上面的三个互动，正好对应这套机制的三个关键环节。
      </p>

      <h2>① 前向过程：加噪是已知的</h2>
      <p>
        给定一张清晰图 <code>x₀</code>，我们按固定的时间表往里掺高斯噪声，
        得到一系列越来越模糊的 <code>x₁, x₂, …, x_T</code>，最后变成一团与原图无关的纯噪声。
        这一步没有任何可学习参数，是一个写死的数学过程：
      </p>
      <pre><code>{`x_t = √(ᾱ_t)·x₀ + √(1−ᾱ_t)·ε,   ε ~ N(0, I)`}</code></pre>
      <p>
        其中 <code>ᾱ_t</code> 随 t 增大而减小——加的噪声越来越多。
        这正是「前向扩散」互动里滑杆在做的事。
      </p>

      <h2>② 反向过程：去噪是学出来的</h2>
      <p>
        真正的模型（一个 <strong>U-Net</strong>）要解决反问题：给定某个带噪的 <code>x_t</code> 和当前步数 t，
        预测「这里面掺了哪些噪声」。训练目标简单到出人意料——
        让网络预测的噪声 <code>ε_θ(x_t, t)</code> 尽量接近真正加进去的 <code>ε</code>：
      </p>
      <pre><code>{`L = E ‖ ε − ε_θ(x_t, t) ‖²`}</code></pre>
      <p>
        生成时，从纯噪声出发，反复「预测噪声 → 减去一点 → 得到更干净的图」，
        循环几十步，图像就从混沌里浮现出来。这就是「反向去噪」播放器展示的全过程。
      </p>

      <h2>③ 为什么在「潜空间」里做</h2>
      <p>
        直接在 512×512 的像素上反复跑 U-Net 太贵。Stable Diffusion 的关键工程取巧是：
        先用一个 <strong>VAE 编码器</strong>把图压成小得多的<em>潜表示（latent）</em>，
        在这个低维空间里完成全部加噪/去噪，最后再用解码器还原成像素图。
        算力因此下降一两个数量级，这也是「Latent Diffusion」名字的由来。
      </p>

      <h2>④ 文字是怎么进来的</h2>
      <p>
        提示词先被 <strong>CLIP 文本编码器</strong>变成一串向量，再通过
        <em>交叉注意力（cross-attention）</em>注入 U-Net 的每一层——
        于是去噪的每一步都「读得到」你的描述。第二个互动里，只加一个词就让成图大变，
        正是文本条件在牵引去噪方向。
      </p>

      <h2>⑤ 引导强度：要多听话</h2>
      <p>
        <strong>无分类器引导（Classifier-Free Guidance, CFG）</strong>让模型同时算一次「带提示」和
        「不带提示」的去噪方向，再把两者的差放大若干倍：
      </p>
      <pre><code>{`ε̂ = ε_uncond + s · (ε_text − ε_uncond)`}</code></pre>
      <p>
        引导强度 <code>s</code> 越大，画面越贴合提示词，但太大（如 20）会过饱和、失真。
        播放器里的 guidance 按钮就是在调这个 <code>s</code>——0 时几乎无视提示词，7 是常用甜点。
      </p>

      <h2>⑥ 步数与速度</h2>
      <p>
        去噪步数越多，结果通常越精细，但越慢。本页的演示用了 50 步；
        现实中的采样器（DDIM、DPM-Solver 等）能把它压到 20 步以内而画质几乎不降。
        而「随机种子」决定最初那团噪声长什么样——换种子，等于换一张构图。
      </p>
    </article>
  );
}
