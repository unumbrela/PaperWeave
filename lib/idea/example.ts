/**
 * 内置示例 ——「医学图像分割的标注高效：半监督降低密集标注成本」。
 *
 * 用途：未配 key 的访客点「载入示例」即可离线走通 诊断→设计→收敛 全流程，
 * 无门槛看到象限图 + 评分卡片的最终效果。也作为有 key 时的输入预填。
 */

import type { Diagnosis, IdeaSet } from "./types";

export const EXAMPLE_INPUT = {
  direction: "医学图像分割的标注高效：用半监督学习降低体素级密集标注成本",
  references:
    "U-Net (Ronneberger et al., 2015): 编码器-解码器 + 跳跃连接，把高分辨率边界细节直接传到解码端，在小样本医学数据上即成默认分割骨干，但训练依赖逐像素/逐体素的密集标注。Mean-Teacher 式一致性正则 (半监督): 对未标注图像施加扰动，约束学生与教师（EMA）预测一致，从而把大量未标注数据转成监督信号。",
  baseline: "U-Net（全监督）/ Mean-Teacher 一致性正则（半监督）",
  resources: "单卡 4090，2 周，公开数据集 ACDC（心脏）/ LA（左心房）",
} as const;

/** 推荐勾选的透镜（与下方 ideas 对应）。 */
export const EXAMPLE_LENS_IDS = ["relax", "crossdomain"] as const;

export const EXAMPLE_DIAGNOSIS: Diagnosis = {
  grounded: true,
  contributions: [
    "U-Net 用跳跃连接把编码端的高分辨率边界直接补给解码端，在数十例量级的小样本医学数据上也能稳定分割，成为该领域的默认骨干",
    "一致性正则（Mean-Teacher）证明：对未标注图像施加扰动并约束预测一致，可把海量未标注数据转化为有效监督信号，显著降低对标注的依赖",
  ],
  assumptions: [
    { id: "a1", text: "全监督隐含「体素级密集标签可得」——默认有放射科专家逐层勾画的金标准" },
    { id: "a2", text: "一致性正则假设「同一图像在不同扰动下预测应一致」即足够监督，未显式利用器官的解剖结构先验" },
  ],
  gaps: [
    { id: "g1", text: "密集体素标注成本极高：需放射科专家逐层勾画，难以规模化扩到新器官/新中心", tag: "标注成本" },
    { id: "g2", text: "标注极少（如 5%）时伪标签噪声大，自训练易陷入确认偏差、错误被不断放大", tag: "可靠性" },
    { id: "g3", text: "跨中心 / 跨设备的域偏移使未标注数据分布漂移，模型泛化明显退化", tag: "泛化" },
    { id: "g4", text: "小病灶 / 低对比边界分割保真度低，前景-背景类别极度不平衡", tag: "保真度" },
  ],
};

export const EXAMPLE_IDEASET: IdeaSet = {
  priority:
    "先做 Idea 1：它把「密集体素标注」这条最贵的约束直接松弛成涂鸦/切片级稀疏标注，工程改动集中在损失与传播项、复用成熟 U-Net 主干，2 周内能在 ACDC 上跑出「标注像素占比 vs Dice」的可量化曲线，正面命中最痛的「标注成本」空白。",
  ideas: [
    {
      id: "idea-1",
      title: "用稀疏标注（scribble/切片级）替代密集体素标注 + 解剖一致性传播",
      motivation:
        "逐体素密集标注是医学分割最贵的一环。放射科专家只在少数切片上随手画几笔（scribble），或只标注体数据中的若干关键切片，远比逐层勾画轻量——关键是如何把这点稀疏监督在整卷上可靠传播。",
      hypothesis:
        "我假设：用 scribble/切片级稀疏标注 + 一项「解剖一致性传播」正则（相邻切片与同器官区域预测应平滑一致），能在标注像素占比降到个位数百分比时逼近全监督 Dice，因为解剖结构的空间连续性提供了标签之外的强先验。",
      experiment:
        "在 ACDC（心脏，逐切片）上把密集标注退化成每例少数切片的 scribble，对比「全监督 U-Net / Mean-Teacher / 本方法」的 Dice 与所用标注像素占比；画一条「标注预算-精度」曲线，单类 100 例量级即可看出是否在 ~10% 标注下不掉点。",
      resources: "复用公开 U-Net 主干，仅加传播正则项，单卡 4090 训练约 2–3 天，落在预算内。",
      risk: "scribble 位置偏置可能让传播只在好标注区域生效；薄壁/低对比处仍弱——先用合成 scribble 做消融，验证传播项对标注位置的鲁棒性。",
      novelty: 4,
      feasibility: 5,
      lens: "约束松弛",
    },
    {
      id: "idea-2",
      title: "引入形状先验 / 配准伪标签作结构监督，抑制半监督的伪标签噪声",
      motivation:
        "一致性正则只约束「扰动下预测一致」，对解剖形状无概念，标注极少时伪标签会长出违反器官拓扑的碎块。器官形状其实是强且廉价的跨域先验。",
      hypothesis:
        "我假设：在半监督损失外加一项「形状先验」监督——用图谱配准得到的伪标签或学到的器官 SDF/形状码约束预测——能压低伪标签噪声、提升边界 Dice，因为形状先验给出了逐体素的结构一致性梯度，把自训练拉回解剖可行域。",
      experiment:
        "在 LA（左心房）上，对比有/无形状先验项的 Mean-Teacher 在 5%/10% 标注下的 Dice 与 Hausdorff 距离；先用单一图谱配准伪标签验证趋势，再换学到的形状码。",
      resources: "图谱配准与 SDF 拟合增加约 30% 训练开销，单卡 4090 两周偏紧但可行。",
      risk: "配准在病变/形变大的病例上不准，会注入错误结构梯度反伤分割；需先评估配准质量并对低置信配准降权。",
      novelty: 4,
      feasibility: 3,
      lens: "跨域引入",
    },
    {
      id: "idea-3",
      title: "不确定性引导的伪标签筛选 + 测试时自适应，缓解跨中心域漂移",
      motivation:
        "跨中心/跨设备时未标注数据分布漂移，固定阈值的伪标签在目标域系统性偏错，模型泛化退化。需要让模型对「该信哪些伪标签」和「目标域长什么样」都有感知。",
      hypothesis:
        "我假设：把「基于预测不确定性（MC-Dropout/熵）筛选可信伪标签」与「测试时对目标域统计量做轻量自适应（更新 BN/少步熵最小化）」组合起来，能在跨中心设置下比固定阈值半监督更稳，因为前者去噪、后者对齐分布，二者互补。",
      experiment:
        "用 ACDC 作源域、另一中心心脏数据作目标域（仅未标注），对比固定阈值半监督 vs 本方法在目标域的 Dice；单源单目标即可看出跨域增益。",
      resources: "不确定性估计与测试时自适应均为轻量增量，单卡 4090 约 3–4 天。",
      risk: "测试时自适应可能在小 batch 上不稳、甚至负迁移；需配早停与回退到源域模型的安全阀。",
      novelty: 3,
      feasibility: 4,
      lens: "组合拼接",
    },
  ],
};
